---
import Layout from './Layout.astro';

const { frontmatter } = Astro.props;
---

<Layout title={`${frontmatter.title} - Barry Mister`}>
  <article class="prose dark:prose-invert max-w-none">
    <header class="mb-8">
      <h1 class="mb-2">{frontmatter.title}</h1>
      <time class="text-gray-600 dark:text-gray-400">
        {new Date(frontmatter.date).toLocaleDateString('en-US', {
          year: 'numeric',
          month: 'long',
          day: 'numeric'
        })}
      </time>
      {frontmatter.description && (
        <p class="mt-4 text-xl text-gray-700 dark:text-gray-300">{frontmatter.description}</p>
      )}
    </header>
    <slot />
  </article>
</Layout>
